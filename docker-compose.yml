services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile

    container_name: chat-backend

    ports:
      - "5001:5000"

    env_file:
      - .env

    restart: unless-stopped